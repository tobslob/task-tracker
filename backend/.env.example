# Default configuration uses a local SQLite database
ENV=dev
DB=sqlite
DB_MEMORY=0

# Allowed CORS origins (comma separated)
BACKEND_CORS_ORIGINS=http://localhost:3000

# To connect to MySQL or PostgreSQL provide a full DATABASE_URL instead.
# Example: mysql+pymysql://user:pass@localhost:3306/database
# DATABASE_URL=
